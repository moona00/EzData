// swift-tools-version: 5.7

import PackageDescription

let package = Package(
    name: "EzData",
    products: [
        .library(
            name: "EzData",
            targets: ["EzData"]),
    ],
    targets: [
        .target(
            name: "EzData",
            dependencies: []),
    ]
)
